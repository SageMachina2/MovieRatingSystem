This java application uses the scanner utility to first ask the user 5 total movies and rate them 1-10. Movie names and ratings are then stored inside a parallel array and caclulate every rating for the average rating. It'll then print feedback messages based on the user's average ratings for the movies, and determine if the user has a favorite or worst movie. Afterwards it'll ask the user to choose their favorite genre, using a switch to streamline the determination of which genre the user typed in.

1. How does a nested if differ from an if-else statement?

    Answer: a nested if is inside of another if statement where as an if else statement executes different blocks of code based on conditions.

2. What are the advantages of using switch over multiple if statements?

    Answer: Using multiple if statements can make the code over complicated than it should be, overall making it less efficient and less readable over using a switch.

3. How does the conditional operator simplify decision-making?

    Answer: nstead of using an if else statement for if the user chooses sci fi, print interstellar, a conditional operator for the movie reccomendation is much more simple to read.

4. What is short-circuit evaluation, and how does it affect the use of logical operators?

    Answer: A short-circuit evaluation is when java stops checking an expression once the result has already been determined. With logical operator && or ||, the evaluation stops after it reaches a conclusion, which will make decisions more efficient and prevent extra conditions from being evalated. 